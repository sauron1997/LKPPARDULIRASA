/**
 * Schedule Repository Interface (Contract)
 */

export const ScheduleRepositoryContract = Symbol('ScheduleRepository');
// Methods: listSessions, getSessionById, createSession, updateSession, deleteSession,
// listSessionAssignments, createSessionAssignments, updateSessionAssignment, removeSessionAssignments
